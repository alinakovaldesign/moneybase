---
description: Start work on a directive. Usage: /directive WALLET-004 (or DESIGN-NNN / CONTENT-NNN)
---

Find `$ARGUMENTS-*.md` by globbing across all areas: `*/directives/$ARGUMENTS-*.md`. Then:

1. Restate the Goal and Acceptance checklist in one short block.
2. List any ambiguity or missing input as questions. STOP and wait if any are blocking.
3. If none are blocking, propose the slice plan (2–4 slices max) and wait for approval.
4. If the directive file is not yet committed, commit it first: `$ARGUMENTS: add directive`.
5. All commits during this work reference $ARGUMENTS.
6. When acceptance is fully met, append a "Learnings" section to the directive file (what you'd tell the next agent) and commit.
